import React from 'react';
import Carousel from '../container/carousel/carousel';

const Homepage = () => {
    return (
        <div>
           <Carousel/>
        </div>
    );
};

export default Homepage;